import type { Character } from '../types';

interface CardsListPros {
  characters: Character[];
}

const CardsList = ({ characters }: CardsListPros) => {
  return <div></div>;
};

export default CardsList;
